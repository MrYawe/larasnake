#include "ia.h"

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

Direction iaSurvive (Board *board, Snake *snake) {
	Direction dir = UP;
/*
	int xMe = snakeGetPos(snake, snakeGetSize(snake)-1, Column);
	int yMe = snakeGetPos(snake, snakeGetSize(snake)-1, Line);

	int bannedX = snakeGetPos(snake, snakeGetSize(snake)-2, Column);
	int bannedY = snakeGetPos(snake, snakeGetSize(snake)-2, Line);

*/

	return dir;
}